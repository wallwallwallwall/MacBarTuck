# MacBarTuck 0.1.3 界面验收

日期：2026-09-10。本轮界面检查通过；真实菜单栏操作的未验收项见 docs/runtime-qa.md。

## 对照结果

对照用户提供的旧版截图与此次新窗口截图，去除了自绘彩色分段控件、密集状态标签、指标概览和大段重复说明。正文统一为系统常规字重，设置项以 13 pt 为主；辅助文字 11 pt。深色外观保留，强调色只用于操作。

## 页面检查

1. 菜单项：原生 Table 和显示方式菜单；名称、图标、选择器及底部数量均可见。搜索匹配为 1 项、无匹配为 0 项、清空后恢复 7 个示例；自动与收起可互相切换。
2. 通用：原生 Form；重置弹窗说明影响，取消后保留状态。预览悬停开关操作前后的真实 UserDefaults 导出校验值一致。
3. 权限与显示器：允许状态为普通文本，显示器名称与类型对齐；没有安全宽度指标或重复状态卡片。本轮截图只包含当前可见的内置显示器。
4. 欢迎：应用图标、短标题、标准继续按钮。
5. 权限设置：两行权限入口，没有宣传标签和重复授权标记。
6. 偏好设置：两个原生复选框，预览登录开关不调用系统登录服务。
7. 完成：简短结果和权限状态，上一步可返回；再次打开应用不强行打断当前步骤。
8. 托盘预览：图标尺寸保持稳定，配色与新窗口协调。

## 截图

所有截图均由 Computer Use 返回的原始窗口截图保存，并逐张检查。标题或页脚标明预览，数据来自 UI 样例，不能作为真实第三方应用识别结果。

- docs/screenshots/items.png：780 x 612。
- docs/screenshots/preferences.png：780 x 612。
- docs/screenshots/settings.png：780 x 612，权限与显示器。
- docs/screenshots/onboarding.png：760 x 592。
- docs/screenshots/onboarding-permissions.png：760 x 592。
- docs/screenshots/onboarding-customize.png：760 x 592。
- docs/screenshots/onboarding-ready.png：760 x 592。
- docs/screenshots/overflow-panel.png：720 x 260。

尺寸包含系统窗口装饰，不将截图尺寸误当成内容区尺寸。截图上的系统录屏指示器不属于应用绘制内容。

## 验证边界

97 项既有回归检查通过，八种预览窗口冒烟通过，另做了上述交互及视觉核对。此次不改变真实菜单项扫描、镜像关联、图标采集和事件转发逻辑；MenuBarItemStore 的修改仅位于预览样例初始化分支。

未据此宣称完整可访问性合规，也未把预览中的“已允许”当成安装版系统授权。原始功能验收仍以真实扫描与系统回执为准。

## 0.1.4 补充

preferences.png 已更新为 0.1.4 实际安装版窗口，展示“应用”分组中的程序坞开关，不是预览数据。该截图只用于设置控件和排版检查；系统菜单的操作验收边界见 docs/app-access.md，其余七张图仍为 0.1.3 标明预览的界面截图。

## 0.1.11 补充

托盘预览改为第三方项目优先显示应用原色图标，应用图标为 28 pt、系统图标为 22 pt，固定槽位为 44 pt。中英文截图均确认图标完整、分隔线不遮挡内容、临时显示圆点和重新收纳按钮不改变槽位尺寸。安全预览不证明真实授权环境中的第三方图标解析或双屏点击结果，相关边界见 docs/runtime-qa.md。

## 0.1.12 补充

用户截图确认旧的“向下箭头 + 1”容易被理解成下载。托盘改用回转归位图标和琥珀色固定宽度按钮，中文直接显示“重新收纳 1 项”，英文显示 `Retuck: 1`；通用设置页沿用相同语义。安装版中英文截图确认文字完整、颜色对比明确、按钮变宽后应用图标和右侧边框均未裁切。固定宽度覆盖常见数量位数，计数变化不会改变项目槽位。

## 0.1.13 补充

用户实机截图显示托盘成为浅灰色大图标条，且右侧出现无意义的小横条。根因是安全预览强制深色、真实 `NSPanel` 仍使用随环境变化的系统材质，同时所有状态都创建横向滚动视图。修正后实机面板固定深色，左侧直接显示实际收纳数量，图标和槽位适度缩小；内容可完整放下时 AX 树不再包含滚动区。空状态与完整状态均检查无文字截断、无多余控件。

最终安装版在内置主屏与 1920 × 1080 扩展屏分别展开真实空托盘，两处均保持相同的深色、边框、状态文字和 `246 × 50` 固定尺寸。安全预览另覆盖彩色项目图标和琥珀色重新收纳按钮；临时签名更新后权限为 `0/2`，因此真实多项目交互仍按运行 QA 标为 `partial`。

## 0.1.14 补充

菜单项页搜索栏右侧增加“应用显示方式”主按钮，用户可在同一页面完成规则选择和执行。通用页删除原有重复操作区，只保留菜单栏、应用、语言、诊断和重置设置；设置窗口不再因为需要执行收纳而跨页往返。中英文按钮均使用常规系统字重，保持单行且没有挤压搜索、刷新和更多操作。

# BarTuck 隐私说明

## 数据处理原则

BarTuck 的菜单栏发现、规则计算、图标截取和点击激活全部在本机执行。项目不包含账号系统、分析 SDK、广告 SDK、崩溃上报或自动网络请求。

## 辅助功能权限

辅助功能权限用于读取菜单栏控件的名称、位置和可执行动作，并在用户点击托盘项目时触发原控件。BarTuck 不读取键盘输入，不记录用户在其他应用中的操作。

## 屏幕录制权限

屏幕录制权限用于截取菜单栏图标对应的小型窗口区域。图片用于当前进程中的托盘显示，不写入磁盘，不上传到网络。缺少该权限时，应用显示授权未就绪，不生成匿名菜单项列表。

## 本机存储

BarTuck 使用 `UserDefaults` 保存以下设置：

- 三态项目规则和已发现项目标识。
- 自动避让、原图标隐藏和悬停展开开关。
- 引导完成状态和菜单栏项目位置元数据。

这些数据通常位于当前用户的 macOS 偏好设置中，应用标识为 `com.bartuck.app`。

## 本地诊断日志

0.1.6 起将时间、进程号、固定事件名、窗口 ID、事务号、数量、错误码及分隔条宽度写入 `~/Library/Logs/BarTuck/`。不记录窗口标题、应用名称、图片、用户输入或凭据。保留两个日志文件，每个最多 1 MiB。日志不自动上传，可由用户在“通用 → 诊断”查看或导出；用户导出的副本不自动清理。

## 系统安全项目

录屏以及音视频隐私状态等系统指示器被强制保持可见。BarTuck 不允许把这些安全状态收进托盘。

## 删除数据

先在偏好页执行“安全重置”恢复菜单栏布局，再退出并删除应用。若还需要删除本机偏好，可在终端执行：

```bash
defaults delete com.bartuck.app
```

辅助功能和屏幕录制授权由 macOS 管理，可在“系统设置 > 隐私与安全性”中移除。

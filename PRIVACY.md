# NotchShelf 隐私说明

## 数据处理原则

NotchShelf 的菜单栏发现、规则计算、图标截取和点击激活全部在本机执行。项目不包含账号系统、分析 SDK、广告 SDK、崩溃上报或自动网络请求。

## 辅助功能权限

辅助功能权限用于读取菜单栏控件的名称、位置和可执行动作，并在用户点击托盘项目时触发原控件。NotchShelf 不读取键盘输入，不记录用户在其他应用中的操作。

## 屏幕录制权限

屏幕录制权限用于截取菜单栏图标对应的小型窗口区域。图片用于当前进程中的托盘显示，不写入磁盘，不上传到网络。缺少该权限时，应用会显示应用图标或系统符号作为替代。

## 本机存储

NotchShelf 使用 `UserDefaults` 保存以下设置：

- 三态项目规则和已发现项目标识。
- 自动避让、原图标隐藏和悬停展开开关。
- 引导完成状态和菜单栏项目位置元数据。

这些数据通常位于当前用户的 macOS 偏好设置中，应用标识为 `com.notchshelf.app`。

## 系统安全项目

录屏以及音视频隐私状态等系统指示器被强制保持可见。NotchShelf 不允许把这些安全状态收进托盘。

## 删除数据

先在偏好页执行“安全重置”恢复菜单栏布局，再退出并删除应用。若还需要删除本机偏好，可在终端执行：

```bash
defaults delete com.notchshelf.app
```

辅助功能和屏幕录制授权由 macOS 管理，可在“系统设置 > 隐私与安全性”中移除。

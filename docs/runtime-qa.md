# 真实功能回归

日期：2026-09-10。整体状态：partial。

## 0.1.0 已授权安装版

- 环境：Apple Silicon，macOS 26.6.2，内置刘海屏与左侧扩展屏；iBar 未运行。
- 两项系统权限已由用户授予，真实应用界面与日志确认授权生效。没有修改 TCC 数据库或绕过授权。
- 已验证：真实窗口启动、双屏枚举、搜索匹配/无匹配/清空、刷新、悬停开关状态持久化、正常退出与再次启动。
- 图标采集有实际图像返回，但部分项目为空；不能把列表数量视为全部识别成功。
- 收纳测试失败：日志多次拒绝目标边缘在显示器外的移动，界面最终显示仍有项目未收纳。已停止重复移动，并关闭布局管理以便诊断；没有清除用户规则或登录启动设置。
- 发现本应用的跨屏镜像被加入待收纳列表；中文控制中心名称导致自身窗口 ID 发布失败；布局缩短分隔项的回调没有连接；主屏标签依赖焦点屏幕而非系统主显示器。

## 0.1.1 修复候选

代码调整：排除自身镜像、按稳定进程标识/自身标题解析窗口、接入分隔项布局生命周期、最宽屏幕有界尺寸、重新收纳等待分隔项尺寸更新、固定主屏身份、重新打开应用显示设置、首次显式录屏请求及进程内防重复。

- 自动化范围：36 项既有规则测试，17 项扫描/分隔项/权限策略检查。
- 回归证据：新增镜像扫描测试在旧扫描行为下失败，排除镜像后通过。
- 权限测试使用注入的系统调用替身，只验证调用策略，不代表 macOS 已实际授予新构建权限。
- UI 冒烟只验证八种预览窗口出现及最小尺寸，不验证点击、隐藏、恢复或文字无截断。
- 原有截图来自 0.1.0 安全预览，不作为 0.1.1 实机验收证据。
- 候选版已单独真实启动，确认 macOS 没有继承旧临时签名的辅助功能/录屏授权，界面显示两项待授权；因此未继续操作原菜单栏。随后退出候选版并重新打开原安装版。
- 本轮已运行：53 项测试通过，原有 36 项及新增 17 项分别在 AddressSanitizer 下通过，Debug/Release 构建通过，Debug 与 DMG 各八种预览窗口冒烟通过。

## 待完成

- 在 0.1.1 原生安装版验证真实隐藏、托盘点击后原菜单出现、菜单关闭后重新隐藏、恢复后的系统窗口位置，以及两块屏幕分别展开托盘。
- 验证显示器热插拔、睡眠唤醒、原应用重启、菜单栏自动隐藏及不同系统版本。
- 验证全新系统授权请求与拒绝/重新授予流程。临时签名更换后可能要求用户重新授权。

为保留用户刚授予的权限，不自动覆盖 `/Applications` 中的已授权 0.1.0；0.1.1 作为独立候选安装包交付。构建、单测通过不代表上述待完成项通过。

## 0.1.2 身份与图标回归

本节为后续迭代记录，上述保留 0.1.0 的状态仅适用于上一轮。

- 本机只读扫描验证：18 条跨屏窗口记录归并为 9 个逻辑项目，保留全部 18 个窗口。录屏指示器出现后的采样为 20 条记录、10 个项目，数量随系统真实项目变化，不硬编码数量。
- 已获授权的测试宿主直接调用生产扫描/采集模块：10 个逻辑项目全部读取到真实菜单栏图像，逐图核对名称。没有用应用备用图标充当成功截图，也没有将结果注入未授权应用。
- 关联依据为本应用定位标记、屏幕、托管进程、相对位置、宽度及明确名称。冲突、同屏重叠或定位信息缺失时不猜测合并；同一应用的多个独立菜单项保留独立规则。
- 新增窗口消失后拒绝点击邻居的回归用例，旧行为先失败、修复后通过；跨屏镜像数量用例亦先复现旧行为失败。
- 补充旧规则迁移、Wi-Fi 手动规则保留、隐私控件常显、匿名规则会话隔离、按鼠标所在屏幕选择点击目标、上下屏坐标、首次引导迁移及跨屏图标采集目标检查。
- 系统未接受新签名权限时显示授权未就绪状态，不列出匿名重复行；所有原图标保留。捕获失败的项目不会被隐藏，状态计数只统计真实菜单栏截图。
- 自动化测试共 97 项；图标核对是只读实机模块测试，不代表安装版已经通过隐藏、点击及恢复的端到端验收。
- 0.1.2 更新到标准应用目录，旧版备份和用户规则保留；临时签名更新后的权限由用户确认。不得把设置中旧记录的开关打开当成新构建已授权。

剩余必测项：最终安装版有效授权后的真实隐藏、左右点击、重新收纳、恢复和两块屏幕分别展开托盘。没有对这些项目作通过结论。

## 0.1.9 双语界面回归

日期：2026-09-11。

- 中文、英文使用同一组 172 个本地化 key，自动检查 key 一致、非空值和格式占位符一致；屏幕录制系统授权说明也分别提供 `InfoPlist.strings`。
- 语言选择写入现有 `UserDefaults`，首次启动按系统语言选择，其他系统语言回退英文。安全预览可使用独立语言参数，不会覆盖用户选择。
- 状态栏菜单与程序坞菜单同时验证中英文标题、设置快捷键、退出入口和程序坞显示状态；切换语言不调用扫描或布局移动。
- 基础测试共 345 项通过，另有 10 项刷新隔离测试及诊断日志检查通过；Apple Silicon Debug 构建通过。
- 中文、英文各 8 个安全预览窗口均成功出现并满足最小尺寸，逐张检查未发现文本遮挡。英文内置显示器名称按选定语言显示，不再沿用系统当前语言。
- 自动化与安全预览不代表真实授权环境中的收纳、原菜单点击、恢复位置和双屏托盘端到端验收；这些边界仍按上文标为待实机复测。

## 0.1.10 菜单栏交互稳定性

日期：2026-09-11。

- 旧安装版日志确认抖动来自单次点击后的第二次拖动与分隔项 `20 -> 3840` 回弹，不是列表刷新或托盘视觉动画。
- 取消自动回藏事务；直接激活不移动，必须揭示的项目保持临时显示，后台自动布局不会擅自收回。托盘与设置页提供显式批量重新收纳。
- 状态栏主图标固定；悬停展开对现有安装执行一次迁移并默认关闭，布局事务期间始终禁用；托盘和按压反馈使用短 `easeOut`，不再缩放图标。
- 新交互策略先以缺失实现的失败测试复现，再实现通过。基础测试 365 项、刷新隔离 10 项及诊断日志轮换通过；Apple Silicon Debug 构建通过。
- `0.1.10 (12)` 最终 Release 安装版已从 DMG 更新到 `/Applications/MacBarTuck.app`，实测可执行文件仅包含 `arm64` 且签名校验通过。权限页识别内置主屏 `1512 × 982` 与 DELL P2419H 扩展屏 `1920 × 1080`，中英文切换、退出重启及两块显示器信息展示正常。
- 安装版进程日志验证：手动点击“重新扫描”只增加一次手动刷新；切到 Finder 再返回后，手动刷新计数保持 `2 -> 2`。上述焦点与刷新路径的菜单栏位置移动事件保持 `0 -> 0`，没有产生 `move.*`、`layout.begin/end`、`layout.rehide` 或非零 `separator.resize`，不会主动移动真实菜单栏项目。
- 当前 build 12 的临时签名没有继承旧安装版的辅助功能和屏幕录制授权，界面显示 `0/2`。因此真实隐藏项点击、一次揭示、原菜单打开以及显式重新收纳仍需在该构建授权生效后分别于主屏和扩展屏复测；这些端到端项目保持 `partial`。

## 0.1.11 彩色图标与托盘尺寸

日期：2026-09-11。

- 新增图标选择回归：第三方项目按“已解析应用图标、扫描时应用图标、菜单栏截图”回退；系统保留项优先菜单栏截图；模板渲染取最终显示图像。测试先在旧的截图优先逻辑下失败，修改后通过。
- 应用图标由 23 pt 视觉区域调整为 28 pt，系统图标为 22 pt；槽位宽度、点击高度和面板高度同步扩大，保持固定尺寸，避免不同应用图标引起布局跳动。
- 初次截图发现最右侧应用图标被旧宽度公式裁切。运行面板与预览改用统一宽度计算，计入图标间距、外边距、分隔线和重新收纳按钮；重新生成的中英文托盘截图已逐张确认无裁切。
- 基础测试 371 项、刷新隔离 10 项及诊断日志轮换通过；Apple Silicon Debug 构建和中英文各 8 个安全预览场景通过，16 张文档截图已更新。
- 安全预览验证了 Shortcuts、Notes、Passwords 等本机应用图标保持原色。真实第三方项目仍取扫描时解析到的自身图标，不使用预览映射。
- `0.1.11 (13)` Release 已从 DMG 更新到 `/Applications/MacBarTuck.app`；包内与安装后检查均为 `arm64`，临时签名校验通过，设置窗口和进程正常启动。
- 安装版启动日志记录 build 13、权限 `0/2`。该进程启动后没有 `layout.begin/end` 或 `move.*`，只设置一次宽度为 0 的静止分隔项，没有移动真实菜单栏项目。
- 当前临时签名权限边界沿用 0.1.10：在安装版重新获得两项授权前，真实第三方彩色图标、隐藏项点击、重新收纳以及主屏/扩展屏端到端体验保持 `partial`。

## 0.1.12 重新收纳按钮

日期：2026-09-11。

- 新增 `panel.retuck.count` 中英文格式检查，旧资源因缺少该 key 先失败，补充实现后通过；基础测试现为 375 项，刷新隔离 10 项与诊断日志轮换继续通过。
- Apple Silicon Debug 构建通过；Debug 与 DMG 内 Release 应用分别完成中文、英文各 8 个安全界面场景，共两轮 16 场景通过。
- 中英文托盘和通用页截图由最终安装版 `0.1.12 (14)` 重新生成。目检确认琥珀色“重新收纳 1 项”/`Retuck: 1` 按钮无截断，回转图标不再呈现下载语义，右侧边框及应用图标无裁切。
- DMG 内与 `/Applications/MacBarTuck.app` 均为 `0.1.12 (14)`、仅 `arm64`，临时签名有效；安装版可执行文件与 DMG 内文件 SHA-256 一致。
- 安装版启动日志显示权限 `0/2`。手动点击一次“重新扫描”后只新增观察性刷新，当前进程保持 `layout=0`、`move=0`，没有触发菜单栏位置变化。
- 因当前安装版尚未重新获得辅助功能和屏幕录制权限，真实临时项目的按钮计数、点击重新收纳及主屏/扩展屏结果仍为 `partial`；安全预览只验证视觉和本地化，不替代真实操作。

## 0.1.13 实机托盘外观

日期：2026-09-11。

- 在已获得两项权限并实际收纳 5 个菜单栏项目的 `0.1.12 (14)` 上复现用户截图：托盘呈浅灰色，正常状态没有文字说明，右侧出现小横条。日志确认 5 个项目均已收纳，问题属于托盘呈现而不是收纳失败。
- 根因是安全预览窗口固定为深色，而真实无边框 `NSPanel` 没有外观约束，`.ultraThinMaterial` 会按实际桌面渲染；同时正常状态仍创建横向滚动视图。新增真实 `NSPanel` 外观测试、空状态宽度测试、5 项无操作区宽度测试和重新收纳操作区宽度测试，旧实现先失败，修复后通过。
- 候选版真实无边框面板检查为深色，空状态完整显示“已收纳 0 项 / 没有已收起的项目”；完整安全预览显示“已收纳 2 项”、3 枚彩色应用图标和“重新收纳 1 项”，AX 树中没有不必要的滚动区。
- 基础测试现为 385 项，刷新隔离 10 项与诊断日志轮换通过；Apple Silicon Debug 构建及中英文各 8 个安全界面场景通过。
- DMG 内 Release 应用为 `0.1.13 (15)`、仅 `arm64`、临时签名有效；直接从 DMG 运行的中文、英文各 8 个安全界面场景再次通过。安装到 `/Applications` 后，可执行文件 SHA-256 与 DMG 内一致。
- 安装版真实空托盘分别在扩展屏 `(-328, 36, 246, 50)` 和内置主屏 `(1186, 38, 246, 50)` 展开。两处均为固定深色，完整显示“已收纳 0 项 / 没有已收起的项目”，没有浅灰系统材质、右侧滚动条残影或文字截断。
- build 15 初次启动进程记录权限 `0/2`；用户重新勾选系统权限并重启后，新进程确认两项均生效。临时签名更新仍可能需要重新授权，不能把系统设置中的旧开关状态当成当前进程已经获得权限。
- 真实 5 项收纳、逐项点击、临时显示和重新收纳将在下一版显式应用布局中复测；本节双屏结果只覆盖托盘定位与真实外观，不把空托盘检查外推为完整菜单栏操作验收。

## 0.1.14 显式应用布局

日期：2026-09-11。

- 已授权的 build 15 中，5 个规则均显示“待收起”但托盘保持 0。只读检查确认会话级按键状态长期为 `left=false`、`right=true`；旧手动布局因此持续延期，日志没有进入 `layout.begin`。这不是权限失效，也不是托盘计数错误。
- 修正后逐项选择“收起”只记录规则，菜单项页提供一次性的“应用显示方式”主按钮；通用页删除重复入口。手动应用忽略残留按键位，后台布局仍保留按压避让。
- 已授权 build 16 首次手动应用成功移动 5 项；重复应用时旧实现仍在 `selected=5 visible=0` 后重新创建布局事务，隐藏分隔项缩短会让项目暂时出现，再产生 5 次重复移动。新增回归在旧实现上以 `RefreshIsolation Code=14` 失败，确认确实触发了隐藏区 resize；修正后实际需要移动的数量为 0 时直接返回并记录 `layout.noop`。
- 基础测试 395 项、刷新隔离 16 项及诊断日志轮换通过；全新 Apple Silicon Debug 构建通过，Debug 与 DMG 内 Release 分别完成中文、英文各 8 个安全界面场景。
- 最终 DMG 与安装版均为 `0.1.14 (16)`、仅 `arm64`，ad-hoc 签名验证通过；安装版可执行文件与 DMG 内文件 SHA-256 一致，DMG 自身的 SHA-256 校验通过。
- 安装版检查确认中英文即时切换、应用版本、设置页和应用菜单中的设置/隐藏/退出入口正常，权限页正确识别内置主屏 `1512 × 982` 与 DELL P2419H 扩展屏 `1920 × 1080`。点击一次“重新扫描”只把当前进程的 `refresh.request` 从 11 增加到 12，没有新增 `layout.begin`、`move.*` 或非零 `separator.resize`。
- 新的 ad-hoc `CDHash` 没有继承旧安装版的两项系统授权，界面正确显示“曾允许 · 当前未生效”。按用户要求，权限配置及依赖权限的最终真实拖动不作为本轮本机验收阻塞；授权入口、状态区分和重启说明已经验证，用户后续自行配置权限。

## 0.1.15 稳定身份、图标与 macOS 27 兼容

日期：2026-09-14。

- 相同稳定 ID 的扫描结果复用原 `MenuBarItem` 与缓存的 `NSImage`；没有展示变化时不再次发布 SwiftUI、状态栏布局和图像回调。刷新隔离回归先在旧行为下以 `RefreshIsolation Code=17` 复现对象替换，修正后 18 项通过，诊断日志轮换、JSON 与大小边界继续通过。
- helper 身份解析到最外层 `.app`，补齐 `CFBundleIconFile`、`CFBundleIconName`、`CFBundleIconFiles` 与 `CFBundleIcons`；输入法使用真实菜单栏图像或键盘符号。基础测试 421 项通过，其中菜单身份与图标检查 83 项。
- macOS 27 及后续选择逐项 Accessibility 扫描与移动，禁用旧隐藏分隔宿主；宽幅复合菜单栏窗口不参与 AX 种子合并，帧宽差异过大的窗口不能匹配同一 AX 子项。隐藏后的刷新保留最后可见恢复帧，避免“全部显示”仍停留在屏外。
- 当前验证机为 Apple Silicon、macOS 26.6.2、Xcode 26.6、macOS SDK 26.5。项目部署目标保持 macOS 15.0，Debug/Release 均固定 `arm64`。Apple 已发布 macOS 27.0 RC 与 Xcode 27 RC，但本机未安装对应系统和 SDK，因此 27 的策略回归与向前兼容编译可验证，27 RC 真机行为和 SDK 27 构建仍为 `UNVERIFIED`。
